'From Squeak3.6beta of ''4 July 2003'' [latest update: #5373] on 1 August 2003 at 12:27:53 am'!"Change Set:		CursorWrapped-arDate:			31 July 2003Author:			Andreas RaabPasteUpMorph>>cursorWrapped: does not wrap due to an oversight in one of the KCP changes. This fixes the problem."!!PasteUpMorph methodsFor: 'cursor' stamp: 'ar 7/31/2003 22:56'!cursorWrapped: aNumber 	"Set the cursor to the given number, modulo the number of items I contain. Fractional cursor values are allowed."	| truncP oldRect newRect |	cursor ~= aNumber 		ifTrue: 			[self hasSubmorphs ifTrue: [oldRect := self selectedRect].			cursor := self asNumber: aNumber.			truncP := cursor truncated.			truncP > submorphs size 				ifTrue: [cursor := submorphs notEmpty ifTrue: [cursor \\ submorphs size] ifFalse: [1]].			truncP < 0 ifTrue: [cursor := 1].			self hasSubmorphs 				ifTrue: 					[newRect := self selectedRect.					self indicateCursor 						ifTrue: 							[self								invalidRect: oldRect;								invalidRect: newRect]]]! !